import React from 'react';

const FaceRecognition = ({ imageUrl }) => {
    return (
        <div className='center'>
            <img alt='will update later' src={imageUrl} />
        </div>
    );
}

export default FaceRecognition;